namespace ASP.NETCoreWebAPI.Dockerize.DockerInstructions;

public class ENTRYPOINT
{
    //ENTRYPOINT instruction is used to determine the command that will be executed when the container starts to run.
    //Preferred syntax in the exec form:
    //ENTRYPOINT ["executable", "param1", "param2"]

    //Example:
    //ENTRYPOINT ["/bin/ping","-c","3"]
}