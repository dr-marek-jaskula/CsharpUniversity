namespace EFCore.EF_Core_advance;

public class Views
{
    //In order to create/use view:
    //1) We need to create w ViewModel (in the folder Data models -> Views)
    //2) We add the db set to the dbContext
    //3) We create an empty migration and customize it (go CustomMigrations -> ViewMigrations)
}